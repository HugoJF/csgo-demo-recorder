demoactions
{
<% Object.entries(commands).forEach(([tick, command]) => { -%>
    "<%- tick %>"
    {
<% Object.entries(command).forEach(([key, value]) => { -%>
        <%- key %> "<%- value %>"
<% }) -%>
    }
<% }) -%>
}